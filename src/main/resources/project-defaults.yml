thorntail:
  deployment:
    test.war:
      web:
        login-config:
          auth-method: KEYCLOAK
        security-constraints:
          - url-pattern: /rest/secured/*
            methods: [GET, POST, PUT, DELETE]
            roles: [test]
  logging:
    loggers:
      kc:
        level: DEBUG